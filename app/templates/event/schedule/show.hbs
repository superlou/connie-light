{{schedule-grid places=model.places sessions=model.sessions
  hourWidth=200
  start=model.start
  finish=model.finish
  moveReservation='moveReservation'
  addEvent='newSession'
  newPlace='newPlace'
  resizeReservation='resizeReservation'
  select='select'
  }}
